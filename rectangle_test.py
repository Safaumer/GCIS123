 
def draw_row(row,height,width,color):
    for a in range(row):
        draw_pixel(height,width,color)
        new_pixel(width)
        return x
    
def test():
    y=draw_pixel("red")
    assert x=="y"

